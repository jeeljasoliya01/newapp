import { ColorMethodDirective } from './color-method.directive';

describe('ColorMethodDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMethodDirective();
    expect(directive).toBeTruthy();
  });
});
