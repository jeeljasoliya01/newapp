import { ToogleDirective } from './toogle.directive';

describe('ToogleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleDirective();
    expect(directive).toBeTruthy();
  });
});
